#include <stdio.h>

/*
Write program that prints its input one word per line

main logic: a word is defined by two different separators --begining and end of word

read a character
while(character C is not end-of-file)
	check if C is a word separator

	check if first character or character within a word
		check if new word, update state to in 
*/

#inculude <stdio.h>
#define IN 1
#define OUT 0

int main()
{
	int c, state;
	
	state = OUT;
	
	While((c = getchar(	


}
