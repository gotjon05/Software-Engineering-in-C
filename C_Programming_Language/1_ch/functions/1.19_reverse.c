/*
while(theres another char)
	save it
reverse what is saved in array

*/

#include <stdio.h>
#define MAXLINE 1000

int getline(char line[], int maxline);
void reverse(char line[]);

int main()
{
	char line[MAXLINE]

	while ((len = getline(line, MAXLINE)) > 0){
		reverse(line);
		printf("%s", line);
	}

}

void reverse(char s[])
{
	


}



